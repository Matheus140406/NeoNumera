# URL da API do backend (FastAPI). Deixe em branco em dev local (usa o proxy do Vite).
# Em produção (Vercel), aponte para o backend hospedado no Railway/Render, ex:
VITE_API_URL=https://seu-backend.up.railway.app/api
